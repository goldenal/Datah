<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adeosunadewale.datah">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Display_list"
            android:label="Customers"></activity>
        <activity android:name=".New_data"
            android:label="New Customer"/>
        <activity android:name=".DisplayInDetails"
            android:label="Profile"/>

        <provider
            android:name=".data.Vprovider"
            android:authorities="com.example.adeosunadewale.datah"
            android:exported="false" />

        <activity android:name=".SplashScreen"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>